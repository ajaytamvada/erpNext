name: Deprecated Azure Container Apps deployment

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This repository now deploys to the Azure VM with the guarded scripts in deployment/."
          echo "The previous Container Apps target is obsolete and this workflow performs no deployment."
